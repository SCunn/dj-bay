  <% @items.limit(5).order(:id).each do |item| %>
<div class="itemhols mb-4">
      <div>
        <div>
          <h4><%= link_to item.album, item %> </h4>
          <div>
            <%= link_to (image_tag item.image_url, :style => "width:100%;"), item %>
            <p><%= number_to_currency(item.price, :unit => "€") %></p>
          </div>
          <div class="card-footer">
            <a href="/cart/<%= item.id %>" class="btn btn-primary">Add to Cart</a>
          </div>
        </div>
      </div>
     
</div>
    <% end %>
